version: "3.8"

services:
  case8-backend:
    image: claude-aiagent-case8-backend:1.0
    container_name: case8-backend
    # ports:
    #   - "${BACKEND_PORT}:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "developer=${DEVELOPER_NAME}"
      - "project.path=${PROJECT_PATH}"
    restart: unless-stopped
    networks:
      - aiagent-network

  case8-frontend:
    image: claude-aiagent-case8-frontend:1.0
    container_name: case8-frontend
    ports:
      - "${FRONTEND_PORT}:5173"
    depends_on:
      - case8-backend
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "developer=${DEVELOPER_NAME}"
      - "project.path=${PROJECT_PATH}"
    restart: unless-stopped
    networks:
      - aiagent-network

networks:
  aiagent-network:
    external: true
