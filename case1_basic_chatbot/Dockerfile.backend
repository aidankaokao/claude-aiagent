FROM python:3.11-slim

WORKDIR /app

# 安裝相依套件
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 複製程式碼
COPY backend/ .

# 建立資料目錄
RUN mkdir -p data

EXPOSE 8000

CMD ["python", "api.py"]
